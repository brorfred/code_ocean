# Large NetCDF files — provided via /data mount at runtime, not baked into image
code/src/abstemp/data/*.nc
code/src/abstemp/data/cmip6/
code/src/abstemp/data/maxmonsst_cmip6/

# Outputs
figs/
results/

# Pixi environment (rebuilt inside image via pixi install)
code/.pixi/

# Build artefacts
**/__pycache__/
**/*.pyc
**/*.pyo

# Dev artefacts
.git/
*.egg-info/
